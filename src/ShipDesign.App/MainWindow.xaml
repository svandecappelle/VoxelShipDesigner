<Window x:Class="ShipDesign.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:h="http://helix-toolkit.org/wpf"
        xmlns:local="clr-namespace:ShipDesign.App"
        mc:Ignorable="d"
        Title="ShipDesign" Height="600" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="230" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <h:HelixViewport3D x:Name="Viewport" Grid.Row="0" Grid.Column="0" ZoomExtentsWhenLoaded="True">
            <h:HelixViewport3D.DefaultCamera>
                <PerspectiveCamera Position="5,5,5" LookDirection="-5,-5,-5" UpDirection="0,1,0" />
            </h:HelixViewport3D.DefaultCamera>
            <h:SunLight />
            <h:GridLinesVisual3D Width="8" Length="8" MinorDistance="1" MajorDistance="1" />
            <ModelVisual3D Content="{Binding ShipModel}" />
        </h:HelixViewport3D>

        <StackPanel Grid.Row="0" Grid.Column="1" Margin="8">
            <TextBlock Text="Modèle" FontWeight="Bold" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding AvailableTemplates}"
                      SelectedItem="{Binding SelectedTemplate}"
                      DisplayMemberPath="Name"
                      Margin="0,0,0,8" />

            <Button Content="Régénérer" Command="{Binding RegenerateCommand}" Padding="8,4" Margin="0,0,0,8" />

            <TextBlock Text="Seed" FontWeight="Bold" Margin="0,0,0,4" />
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SeedInput, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
                <Button Grid.Column="1" Content="Aller" Command="{Binding GenerateWithSeedCommand}" Padding="8,2" Margin="4,0,0,0" />
            </Grid>

            <Button Content="Exporter..." Command="{Binding ExportCommand}" Padding="8,4" Margin="0,0,0,16" />

            <TextBlock Text="Pièces assemblées" FontWeight="Bold" Margin="0,0,0,4" />
            <ListBox ItemsSource="{Binding PartsSummary}"
                     SelectedItem="{Binding SelectedPartEntry}"
                     DisplayMemberPath="Text"
                     Height="140"
                     FontSize="11"
                     Margin="0,0,0,8" />

            <Button Content="Remplacer la pièce" Command="{Binding ReplacePartCommand}" Padding="8,4" Margin="0,0,0,8" />

            <TextBlock Margin="0,0,0,4">
                <Run Text="Échelle : " FontWeight="Bold" />
                <Run Text="{Binding SelectedPartScale, StringFormat='x{0:0.00}', Mode=OneWay}" />
            </TextBlock>
            <Slider Minimum="0.5" Maximum="2" Value="{Binding SelectedPartScale}"
                    TickFrequency="0.1" SmallChange="0.05" LargeChange="0.25"
                    IsSnapToTickEnabled="True" IsMoveToPointEnabled="True" />
        </StackPanel>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Margin="8" TextWrapping="Wrap" Text="{Binding StatusText}" />
    </Grid>
</Window>
